library changelog: false, identifier: "gebit-jenkins@${GEBIT_BUILD_BRANCH}"

buildSimplePipeline(this) {
    stage('Checkout') {
        checkoutProject()
    }
    stage('Build') {
        println 'building...'
    }
}

